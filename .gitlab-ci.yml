stages:
  - check
  - test
  - install

default:
  tags:
    - docker

variables:
  CODECOV_TOKEN: "b524d434-9189-48fc-a919-72e40de8cd22"

before_script:
  - micromamba activate tableExtra

check:
  stage: check
  image: gitlab-research.centralesupelec.fr:4567/2019pradaty/tableextra_docker:3243c142d6f2118ec0dbafb2e5ff6a0381474879
  script:
    - Rscript -e 'if (!identical(devtools::check(document = FALSE, args = "--no-tests")[["errors"]], character(0))) stop("Check with Errors")'
  only:
    refs:
      - master
      - dev

test: 
  stage: test
  image: gitlab-research.centralesupelec.fr:4567/2019pradaty/tableextra_docker:3243c142d6f2118ec0dbafb2e5ff6a0381474879
  script:
    - Rscript -e 'if (any(as.data.frame(devtools::test())[["failed"]] > 0)) stop("Some tests failed.")'
    - Rscript -e 'covr::codecov(token="b524d434-9189-48fc-a919-72e40de8cd22")'
    - bash <(curl -s https://codecov.io/bash)
  only:
    refs:
      - master
      - dev

install:
  stage: check
  image: gitlab-research.centralesupelec.fr:4567/2019pradaty/tableextra_docker:3243c142d6f2118ec0dbafb2e5ff6a0381474879
  script:
    - Rscript -e 'devtools::build(binary=T)'
    - Rscript -e 'devtools::install()'
  only:
    refs:
      - master
      - dev
